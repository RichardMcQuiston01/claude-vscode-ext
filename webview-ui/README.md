# webview-ui

Vite + React + TailwindCSS app that renders the schema-driven settings form
inside the extension's webview panel. Built by `npm run build` at the repo
root (bundles to `../dist/webview`); see the root [README.md](../README.md)
and [CLAUDE.md](../CLAUDE.md) for the overall project.
